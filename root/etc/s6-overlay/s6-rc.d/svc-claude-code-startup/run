#!/usr/bin/with-contenv bash
# shellcheck shell=bash

# This runs as the abc user
# Execute startup script directly from image (not from /config to avoid stale cached versions)
exec s6-setuidgid abc /defaults/startup.sh