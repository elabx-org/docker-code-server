# GitHub Repository (update with your username/repo)
GITHUB_REPOSITORY=yourusername/docker-code-server

# Timezone
TZ=America/New_York

# Code-server authentication (optional)
CODE_SERVER_PASSWORD=your-secure-password

# Sudo password for container (optional)
SUDO_PASSWORD=your-sudo-password

# User/Group IDs (adjust based on your system)
PUID=1000
PGID=1000

# Proxy domain for subdomain proxying (optional)
# Allows accessing internal services via subdomains
# Example: PROXY_DOMAIN=code.mydomain.com
PROXY_DOMAIN=

# VS Code extensions to install on startup (optional)
# Comma-separated list of extension IDs from Open VSX marketplace
# Example: VSCODE_EXTENSIONS=ms-python.python,dbaeumer.vscode-eslint,eamodio.gitlens
# Alternative: Create config/extensions.txt with one extension per line
VSCODE_EXTENSIONS=

# OpenAI API Key (optional)
# Get your API key from https://platform.openai.com/api-keys
# This allows you to use OpenAI's API for AI development
OPENAI_API_KEY=

# Google API Key (optional)
# Get your API key from https://aistudio.google.com/apikey
# Alternative to OAuth for Gemini CLI authentication
# Note: OAuth sign-in with Google account is recommended (free tier available)
GOOGLE_API_KEY=

# Code-Server URL (optional, but recommended for OAuth authentication)
# Your public code-server URL for automatic OAuth proxy URL conversion
# Example: CODE_SERVER_URL=https://code.example.com
# This enables seamless OAuth authentication for gh, codex, and other CLI tools
CODE_SERVER_URL=